# ==============================================================================
# SAFE DEPENDENCIES FOR KAGGLE NOTEBOOKS (2x T4 GPUs)
# ==============================================================================
# WARNING: DO NOT run `pip install -r requirements.txt` on Kaggle!
# The legacy requirements.txt downgrades PyTorch and causes CUDA version mismatch.
#
# Recommended installation sequence at Cell 1 of Kaggle Notebook:
#   !pip uninstall -y -q torchaudio
#   !pip install -q --no-cache-dir -r requirements_kaggle.txt
# ==============================================================================

transformers>=4.45.0
accelerate>=0.26.0
sentencepiece
protobuf
tiktoken
qwen_vl_utils
pyyaml
tqdm
huggingface_hub
pandas
